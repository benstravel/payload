# Postgres connection string. In Coolify, use the internal service name Coolify assigns
# your Postgres resource as the host, e.g. postgres://user:pass@postgres:5432/payload
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/payload

# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=YOUR_SECRET_HERE
